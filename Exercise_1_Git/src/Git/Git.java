

public class Git {

	public void main (String[] args) {
		
	}
	// Should return true if you can reach exactly the goal(not more and not less) with the number of small and big bricks
	//if you cannot reach exactly the goal with the bricks it should return false
	
	/**
	 * @param smallBricks is 1 long
	 * @param bigBricks is 5 long
	 * @param goal 
	 * @return
	 */
	public boolean makeBricks(int smallBricks, int bigBricks, int goal) {
		return true;
	}
}
